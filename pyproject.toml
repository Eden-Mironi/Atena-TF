[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "gym_atena"
version = "0.0.1"
dependencies = ["gym", "numpy", "pandas"]

[tool.setuptools.packages.find]
where = ["."]
